import requests


def send_request():
    method = "{method}"
    url = "{url}"
    headers = {headers}
    data = "{data}"
    requests.request(method=method, url=url, headers=headers, data=data)


if __name__ == '__main__':
    send_request()